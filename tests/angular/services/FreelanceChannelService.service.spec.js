ngDescribe({
    name: 'Test FreelanceChannelServiceService',
    modules: 'app',
    inject: 'FreelanceChannelServiceService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
