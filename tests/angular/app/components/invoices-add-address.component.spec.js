ngDescribe({
    name: 'Test invoices-add-address component',
    modules: 'app',
    element: '<invoices-add-address></invoices-add-address>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
