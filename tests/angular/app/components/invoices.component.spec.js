ngDescribe({
    name: 'Test invoices component',
    modules: 'app',
    element: '<invoices></invoices>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
