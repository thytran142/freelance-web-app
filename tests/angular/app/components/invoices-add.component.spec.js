ngDescribe({
    name: 'Test invoices-add component',
    modules: 'app',
    element: '<invoices-add></invoices-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
